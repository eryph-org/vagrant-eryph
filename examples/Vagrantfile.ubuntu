# Ubuntu Linux catlet example - using new Ruby-style syntax
Vagrant.configure("2") do |config|  
  config.vm.provider :eryph do |eryph|
    # Ultra-minimal configuration - only parent is required
    eryph.parent = "dbosoft/ubuntu-22.04/latest"
    # project defaults to "default", all other settings use sensible defaults
  end
end